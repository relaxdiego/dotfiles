---
description: Work in poteto mode (alias for the poteto-mode skill)
---

Load the `poteto-mode` skill, read it in full, and follow it for the rest of
this task. opencode registers pstack's skills under their bare frontmatter
names, so it is `poteto-mode` here and not the `pstack:poteto-mode` that
Claude Code uses.

The task:

$ARGUMENTS
