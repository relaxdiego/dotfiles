#!/bin/bash -e
{{ if eq .chezmoi.os "linux" -}}
    {{ if eq .chezmoi.osRelease.id "ubuntu" -}}
    which ack 1>/dev/null || sudo apt install ack -y
    {{ end -}}

{{ else if eq .chezmoi.os "darwin" -}}
which ack 1>/dev/null || brew install ack

{{ end -}}
