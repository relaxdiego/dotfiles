LICENSE
README.md
AGENTS.md
CLAUDE.md
docs/**
scripts/**
logo.png
screenshot*.png
tmux*.log
tags
.config/nvim/plugin/packer_compiled.lua
.config/nvim/lua/.luarc.json
.config/nvim/.luarc.json
.config/nvim/spell/*
.bashrc.d/*chezmoi-ignored.bashrc*
.DS_Store
.claude/*
!.claude/CLAUDE.md
!.claude/settings.json
!.claude/statusline.sh
!.claude/themes
!.claude/themes/**
# .claude/* is a single-level glob, so it never reaches the style files.
# Deny the directory's contents outright and re-admit only the one style
# .chezmoiexternal.toml installs. Last matching pattern wins, so the order
# of these two lines is what makes it work.
.claude/output-styles/**
!.claude/output-styles/no-slop.md
{{ if (and (hasKey . "agent") .agent) }}
!.claude/skills
!.claude/skills/**
{{ end }}
{{ if ne .chezmoi.os "darwin" }}
.config/iterm2/**
.local/bin/ghostty-thicken
{{ end }}
{{ if not (and (hasKey . "agent") .agent) }}
.local/bin/git-credential-gh-org
.local/bin/gh
.local/bin/guest
.local/share/guest/**
.local/share/agent-docs/github-auth.md
{{ end }}
{{ if ne .chezmoi.os "linux" }}
.local/bin/guest
.local/share/guest/**
{{ end }}
