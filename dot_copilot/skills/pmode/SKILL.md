---
name: pmode
description: Work in poteto mode (alias for the poteto-mode skill). Use when the user says pmode, /pmode, or poteto mode.
---
# pmode

Alias. Copilot CLI has no custom slash commands, so `pmode` is a skill rather
than a `/pmode` command; the user invokes it by name.

Call the `skill` tool with `skill="poteto-mode"`, read it in full, and follow
it for the rest of this task. Whatever the user said after `pmode` is the task.

Do nothing else here. This file adds no rules of its own.
